import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.kevin.tchinda',
  appName: 'kevin-tchinda',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
